import React from 'react';
import { Nav } from 'react-bootstrap';

const PackageNavbar = (props) =>
{
    // const { id } = props.service;
    return (
        <div>
            <Nav className="m-auto bg-dark shadow py-3">

            </Nav>
        </div>
    );
};

export default PackageNavbar;